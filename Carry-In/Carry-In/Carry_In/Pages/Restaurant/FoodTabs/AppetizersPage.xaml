<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Carry_In.Pages.Restaurant;"
             x:Class="Carry_In.Pages.Restaurant.FoodTabs.AppetizersPage"
             Title="Appetizers">
  <ListView x:Name="appetizersList" ItemsSource="{Binding Appetizers}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <local:FoodItemCell Text="{Binding Name}"
                  Detail="{Binding Description}"
                  TextColor="{StaticResource primary_dark}"
                  DetailColor="{StaticResource primary}"
                  FoodItem="{Binding FoodItem}">
        </local:FoodItemCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>